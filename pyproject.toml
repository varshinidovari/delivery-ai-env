[project]
name = "delivery-ai-env"
version = "0.1.0"
description = "AI Delivery Optimization Environment"
dependencies = [
    "flask",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:app"
