[project]
name = "AI Delivery Optimization"
version = "0.1.0"
description = "AI Smart Delivery Optimization Environment"

dependencies = [
    "flask",
    "numpy",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:app"
